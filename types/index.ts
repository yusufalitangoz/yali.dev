export type LinkProps = {
  href: string;
  label: string;
};

export type SocialProps = {
  name: string;
  url: string;
  icon: string;
};

export type TechStackGroupProps = {
  title: string;
  items: string[];
};
